$(document).ready(function() {
    $(".next__btn").click(function() {		// 모든 <button>요소에 click 이벤트를 설정함.
      alert("앞으로가기 버튼")
    });
    $(".prev__btn").click(function() {		// 모든 <button>요소에 click 이벤트를 설정함.
        alert("뒤로가기 버튼!")
      });
});